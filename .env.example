# CHECK24 Login Testing Configuration

# Base URL für CHECK24 Login
CHECK24_BASE_URL=https://accounts.check24.com

# Test-Account Zugangsdaten
# Passwort: Direkt in tests/fixtures/accounts.ts definiert (TEST_PASSWORD)
#
# Multi-Account System
# WICHTIG: Login Challenge ≠ 2FA
# - Login Challenge: Sicherheitsprüfung bei unbekanntem Gerät (kommt immer)
# - 2FA: Nur wenn in Account-Einstellungen aktiviert (optional)
#
# Verfügbare Accounts (siehe tests/fixtures/accounts.ts):
# - EMAIL_ONLY: loyaltytesting+automatedtesting1@check24.de (Nur E-Mail)
# - EMAIL_PHONE: loyaltytesting+automatedtestingcombinedaccount@check24.de (E-Mail + Phone)
# - EMAIL_PHONE_2FA: loyaltytesting+automatedtestingcombinedaccountwith2fa@check24.de (E-Mail + Phone + 2FA)

# Microsoft Graph API für E-Mail-Zugriff (2FA TAN-Codes)
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
AZURE_CLIENT_SECRET=your-client-secret
EMAIL_ACCOUNT=loyaltytesting@check24.de

# Slack Integration
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Dashboard-Konfiguration
DASHBOARD_BASE_URL=http://localhost:3000
API_PORT=4000

# 24/7 Monitoring
# Test-Intervall in Minuten (z.B. 15 = alle 15 Minuten)
TEST_INTERVAL_MINUTES=15

# Playwright Konfiguration
PLAYWRIGHT_HEADLESS=true
PLAYWRIGHT_VIDEO=true
PLAYWRIGHT_SCREENSHOT=on-failure

# Database
DATABASE_PATH=./data/testresults.db

# Automatisches Löschen alter Test-Runs (in Tagen)
# Test-Runs älter als X Tage werden automatisch gelöscht (täglich um 3 Uhr)
CLEANUP_DAYS=4

# Timeouts (in Millisekunden)
DEFAULT_TIMEOUT=1200000      # 20 Minuten (für Tests mit Login-Challenge/E-Mail-Abruf)
NAVIGATION_TIMEOUT=60000
